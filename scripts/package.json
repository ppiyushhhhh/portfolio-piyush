{
  "name": "portfolio-health-report",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Daily website health report generator (HTTP + SSL + DNS + Lighthouse -> PDF -> Email)",
  "scripts": {
    "report": "node generate-report.mjs"
  },
  "dependencies": {
    "pdfkit": "^0.15.0",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "lighthouse": "^12.2.1"
  }
}
